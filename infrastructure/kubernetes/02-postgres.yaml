apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: autonomous-enterprise
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: autonomous-enterprise
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
